version: '3.8'

services:
  backend:
    build: ./backend
    container_name: smartcart-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/app
      - /app/node_modules
    restart: unless-stopped
